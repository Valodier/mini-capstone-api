@images.map do |image|
  {
    id: image.id,
    url: image.url,
    prduct_id: image.product_id,
    created_at: image.created_at,
    updated_at: image.updated_at
    #url: image_url(image, format: :json)
  }
end
